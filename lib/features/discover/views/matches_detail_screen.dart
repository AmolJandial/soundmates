import 'package:flutter/material.dart';

class MatchesDetailScreen extends StatelessWidget {
  const MatchesDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SafeArea(child: Center(child: Text('Matches Detail Screen'))));
  }
}
