import 'package:flutter/material.dart';

class NormalInfoPage extends StatelessWidget {
  const NormalInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
